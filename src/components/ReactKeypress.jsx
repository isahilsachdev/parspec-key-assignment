import React from 'react';
import '../App.css';

const ReactKeyPress = ({children}) => {
  return (
    <div className='keypress-container'>
      {children}
    </div>
  )
}

export default ReactKeyPress